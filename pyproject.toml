[project]
name = "serverboizbot"
version = "0.1.0"
description = "Anything goes discord bot for ServerBoiz"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "discord-py[voice]>=2.6.4",
    "dotenv>=0.9.9",
]

[dependency-groups]
dev = [
    "black>=25.12.0",
]
